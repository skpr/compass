name: "📋 Collector: Build and Test"

on:
  push

jobs:
  collector_build:
    runs-on: ubuntu-latest
    steps:
      - name: ⬇️ Git clone the repository
        uses: actions/checkout@v4
      - name: 📦 Build
        run: |
          docker build -t docker.io/skpr/compass-collector:latest collector
